- This is a project of a one-way bridge that allows activitypub users to follow the bluesky users.
- Good starting point is to check out `preliminary_planning.md` - this contains some useful information that was found beforehand, but practical means of this research was not evaluated, thus it should not be used as a strong guidelines, and we should take the oppurtunities for improvements and reliable shortcuts when discovered and possible.
- Keep your further planning and progress in `planning_and_progress.md` file. Keep this file updated but clean, don't overload it with historical information that is not relevant anymore!
- This project and the agents working on it should use a "we don't guess" approach. Everything should be covered by tests, either unit, integration and end-to-end, at the later stages of project there should be a fully automated tests that check the main functionality of actually subscribing and receiving the post with live bluesky and activitypub servers and accounts
- Live E2E runbook and known quirks are in `live_testing.md`
- Local machine-readable credentials for live E2E are in `test_credentials.json` (gitignored)
